{
  "name": "triv101-api",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev --local",
    "migrate:local": "wrangler d1 execute triv101 --local --file=migrations/0001_init.sql && wrangler d1 execute triv101 --local --file=migrations/0002_seed_prompts.sql",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.116.0"
  }
}
